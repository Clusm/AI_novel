warning: in the working copy of 'src/tasks.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/tasks.py b/src/tasks.py[m
[1mindex 9b6ef23..2f85574 100644[m
[1m--- a/src/tasks.py[m
[1m+++ b/src/tasks.py[m
[36m@@ -1,15 +1,35 @@[m
[32m+[m[32m"""[m
[32m+[m[32m任务模块 - 构建 CrewAI 任务链[m
[32m+[m
[32m+[m[32m核心功能：[m
[32m+[m[32m1. 创建 4 个协作任务：大纲优化、世界观守护、正文撰写、终审润色[m
[32m+[m[32m2. 支持两种文风模式：标准模式（传统正剧）、番茄模式（快节奏爽文）[m
[32m+[m[32m3. 智能大纲提取：自动识别详细细纲，跳过优化步骤[m
[32m+[m[32m4. 上下文管理：控制剧情圣经、事实台账的长度，避免 embedding 超限[m
[32m+[m[32m"""[m
[32m+[m
 from crewai import Task[m
 import os[m
 import re[m
 [m
[31m-# CrewAI 记忆检索时会把「任务 description + context」整段作为 query 发给 Embedding API。[m
[31m-# 因此需要控制 description 的长度，避免 embedding 查询超限。[m
[32m+[m[32m# CrewAI 记忆检索时会把「任务 description + context」整段作为 query 发给 Embedding API[m
[32m+[m[32m# 因此需要控制 description 的长度，避免 embedding 查询超限[m
 STORY_BIBLE_MAX_CHARS = 2600[m
 STORY_BIBLE_MAX_CHARS_TOMATO = 3200[m
 OUTLINE_EXCERPT_MAX_CHARS = 1200[m
 [m
 [m
 def _clamp_with_suffix(text: str, max_chars: int, suffix: str) -> str:[m
[32m+[m[32m    """[m
[32m+[m[32m    截断文本到指定长度，末尾添加后缀[m
[32m+[m
[32m+[m[32m    参数：[m
[32m+[m[32m    - text: 原始文本[m
[32m+[m[32m    - max_chars: 最大字符数[m
[32m+[m[32m    - suffix: 截断后添加的后缀[m
[32m+[m
[32m+[m[32m    返回：截断后的文本[m
[32m+[m[32m    """[m
     if max_chars <= 0:[m
         return ""[m
     if len(text) <= max_chars:[m
[36m@@ -20,7 +40,18 @@[m [mdef _clamp_with_suffix(text: str, max_chars: int, suffix: str) -> str:[m
 [m
 [m
 def _extract_outline_excerpt(user_outline: str, current_chapter: int, max_chars: int) -> tuple[str, bool]:[m
[31m-    """提取大纲片段，并返回是否为详细细纲"""[m
[32m+[m[32m    """[m
[32m+[m[32m    从大纲中提取当前章节的片段[m
[32m+[m
[32m+[m[32m    参数：[m
[32m+[m[32m    - user_outline: 用户提供的完整大纲[m
[32m+[m[32m    - current_chapter: 当前章节序号[m
[32m+[m[32m    - max_chars: 最大字符数[m
[32m+[m
[32m+[m[32m    返回：(章节片段, 是否为详细细纲)[m
[32m+[m[32m    - 章节片段：提取的大纲内容[m
[32m+[m[32m    - 是否为详细细纲：True 表示提取到了完整的章节大纲，可跳过优化步骤[m
[32m+[m[32m    """[m
     text = (user_outline or "").strip()[m
     if not text:[m
         return "", False[m
[36m@@ -28,16 +59,16 @@[m [mdef _extract_outline_excerpt(user_outline: str, current_chapter: int, max_chars:[m
     # 尝试精准匹配 "#### 第N章" 或 "**第N章" 这种大纲标题[m
     chapter_pattern = rf"(?m)^#{2,4}\s*(\*{0,2}第\s*{current_chapter}\s*章[^\n]*)"[m
     match = re.search(chapter_pattern, text)[m
[31m-    [m
[32m+[m
     chapter_content = ""[m
     is_detailed = False[m
[31m-    [m
[32m+[m
     if match:[m
         start_idx = match.start()[m
         # 找下一章的标题作为结束[m
         next_chapter_pattern = rf"(?m)^#{2,4}\s*(\*{0,2}第\s*{current_chapter + 1}\s*章)"[m
         next_match = re.search(next_chapter_pattern, text[start_idx:])[m
[31m-        [m
[32m+[m
         if next_match:[m
             end_idx = start_idx + next_match.start()[m
             chapter_content = text[start_idx:end_idx].strip()[m
[36m@@ -50,7 +81,7 @@[m [mdef _extract_outline_excerpt(user_outline: str, current_chapter: int, max_chars:[m
                  chapter_content = text[start_idx:end_idx].strip()[m
             else:[m
                  chapter_content = text[start_idx:].strip()[m
[31m-    [m
[32m+[m
     # 如果没找到精准章节，回退到模糊搜索（但不认为是详细细纲）[m
     if not chapter_content:[m
         return _extract_outline_excerpt_legacy(text, current_chapter, max_chars), False[m
[36m@@ -61,17 +92,20 @@[m [mdef _extract_outline_excerpt(user_outline: str, current_chapter: int, max_chars:[m
         # 如果太长，适当截断，但保留核心[m
         if len(chapter_content) > max_chars:[m
              chapter_content = _clamp_with_suffix(chapter_content, max_chars, "…")[m
[31m-    [m
[32m+[m
     return chapter_content, is_detailed[m
 [m
[32m+[m
 def _extract_outline_excerpt_legacy(text: str, current_chapter: int, max_chars: int) -> str:[m
[31m-    """旧的提取逻辑，用于模糊匹配"""[m
[32m+[m[32m    """[m
[32m+[m[32m    旧版大纲提取逻辑 - 模糊匹配[m
[32m+[m
[32m+[m[32m    当精准匹配失败时使用，通过正则搜索章节关键词定位[m
[32m+[m[32m    """[m
     header_budget = min(600, max_chars // 2)[m
     sep = "\n\n【章节相关片段】\n"[m
     suffix = "…（大纲摘录已截断）"[m
[31m-    [m
[31m-    # ... (原有逻辑简化或直接复用)[m
[31m-    # 为节省篇幅，这里简化处理，实际可以直接保留原有逻辑[m
[32m+[m
     loose_patterns = [[m
             rf"第\s*{int(current_chapter)}\s*章",[m
             rf"章节\s*{int(current_chapter)}\b",[m
[36m@@ -86,15 +120,31 @@[m [mdef _extract_outline_excerpt_legacy(text: str, current_chapter: int, max_chars:[m
         end = min(len(text), m.end() + 1400)[m
         chapter_slice = text[start:end].strip()[m
         break[m
[31m-        [m
[32m+[m
     if not chapter_slice:[m
         return _clamp_with_suffix(text, max_chars, suffix)[m
[31m-        [m
[32m+[m
     return _clamp_with_suffix(chapter_slice, max_chars, suffix)[m
 [m
 [m
 def _build_style_profile(writing_style: str) -> dict:[m
[31m-    """集中管理文风相关提示与字数边界，避免 create_tasks 里散落分支。"""[m
[32m+[m[32m    """[m
[32m+[m[32m    构建文风配置[m
[32m+[m
[32m+[m[32m    根据文风模式返回对应的提示词和字数限制：[m
[32m+[m[32m    - tomato（番茄模式）：快节奏、高爽点密度、短段落[m
[32m+[m[32m    - standard（标准模式）：稳健节奏、逻辑严密、沉浸体验[m
[32m+[m
[32m+[m[32m    返回：{[m
[32m+[m[32m        style_instruction: 文风总要求[m
[32m+[m[32m        outline_style_instruction: 大纲优化师的额外要求[m
[32m+[m[32m        guardian_style_instruction: 世界观守护者的额外要求[m
[32m+[m[32m        writer_style_instruction: 主写手的额外要求[m
[32m+[m[32m        finalizer_style_instruction: 审校专家的额外要求[m
[32m+[m[32m        min_chars: 最小字数[m
[32m+[m[32m        max_chars: 最大字数[m
[32m+[m[32m    }[m
[32m+[m[32m    """[m
     if writing_style == "tomato":[m
         min_chars = int(os.getenv("CHAPTER_MIN_CHARS_TOMATO", "2100"))[m
         max_chars = int(os.getenv("CHAPTER_MAX_CHARS_TOMATO", "2600"))[m
[36m@@ -109,9 +159,9 @@[m [mdef _build_style_profile(writing_style: str) -> dict:[m
             ),[m
             "outline_style_instruction": ([m
                 "\n\n【提纲输出要求（番茄模式加严）】\n"[m
[31m-                "1) 给出“开头钩子”（1-3句，直接冲突/对白起笔）；\n"[m
[31m-                "2) 给出“爽点节奏表”，按每500字一个节点列出：触发点/情绪点/推进结果；\n"[m
[31m-                "3) 给出“章末断章点”（一句话）；\n"[m
[32m+[m[32m                "1) 给出「开头钩子」（1-3句，直接冲突/对白起笔）；\n"[m
[32m+[m[32m                "2) 给出「爽点节奏表」，按每500字一个节点列出：触发点/情绪点/推进结果；\n"[m
[32m+[m[32m                "3) 给出「章末断章点」（一句话）；\n"[m
             ),[m
             "guardian_style_instruction": ([m
                 "\n\n【守护者输出要求（番茄模式加严）】\n"[m
[36m@@ -166,21 +216,45 @@[m [mdef create_tasks([m
     compact_mode=False,[m
     writing_style="standard",[m
 ):[m
[31m-    """构建章节生成任务链：提纲(可选) -> 守护者 -> 主写 -> 终审。"""[m
[32m+[m[32m    """[m
[32m+[m[32m    构建章节生成任务链[m
[32m+[m
[32m+[m[32m    任务流程：[m
[32m+[m[32m    1. 大纲动态优化师（可选）：细化大纲、生成爽点清单[m
[32m+[m[32m       - 若已有详细细纲，则跳过此步骤[m
[32m+[m[32m    2. 人物与世界观守护者：提取人物卡、检查一致性[m
[32m+[m[32m    3. 章节主写手：撰写正文[m
[32m+[m[32m    4. 终极审校专家：润色、检查一致性、移除摘要块[m
[32m+[m
[32m+[m[32m    参数：[m
[32m+[m[32m    - agents: Agent 列表 [大纲优化师, 世界观守护者, 主写手, 审校专家][m
[32m+[m[32m    - story_bible: 剧情圣经（全局设定）[m
[32m+[m[32m    - user_outline: 用户大纲[m
[32m+[m[32m    - current_chapter: 当前章节序号[m
[32m+[m[32m    - previous_chapter_content: 上一章承接信息[m
[32m+[m[32m    - canon_context: 最近章节事实台账[m
[32m+[m[32m    - compact_mode: 精简模式（失败降级时使用）[m
[32m+[m[32m    - writing_style: 文风模式（standard/tomato）[m
[32m+[m
[32m+[m[32m    返回：Task 列表[m
[32m+[m[32m    """[m
     tasks = [][m
 [m
[32m+[m[32m    # 控制剧情圣经长度，避免 embedding 超限[m
     bible_for_desc = (story_bible or "").strip()[m
     bible_limit = STORY_BIBLE_MAX_CHARS_TOMATO if writing_style == "tomato" else STORY_BIBLE_MAX_CHARS[m
     if len(bible_for_desc) > bible_limit:[m
         bible_for_desc = bible_for_desc[:bible_limit] + "…（剧情圣经已截断）"[m
 [m
[32m+[m[32m    # 提取当前章节的大纲片段[m
     outline_excerpt, is_detailed_outline = _extract_outline_excerpt(user_outline, current_chapter, OUTLINE_EXCERPT_MAX_CHARS)[m
 [m
     # 上一章承接信息（用于保持连贯性）[m
     prev_info = ""[m
     if previous_chapter_content:[m
         prev_info = f"\n\n【上一章承接信息（只用于无缝接续，禁止复述前情）】\n{previous_chapter_content.strip()}"[m
[31m-    [m
[32m+[m
[32m+[m[32m    # 构建基础上下文[m
     base_context = ""[m
     if outline_excerpt:[m
         base_context += f"\n\n【本章大纲指引（必须严格执行）】\n{outline_excerpt}"[m
[36m@@ -190,7 +264,8 @@[m [mdef create_tasks([m
     if canon_context:[m
         canon_limit = 2800 if writing_style == "tomato" else 1800[m
         base_context += f"\n\n【最近章节事实台账（强连续性输入）】\n{canon_context[:canon_limit]}"[m
[31m-    [m
[32m+[m
[32m+[m[32m    # 获取文风配置[m
     style_profile = _build_style_profile(writing_style)[m
     style_instruction = style_profile["style_instruction"][m
     outline_style_instruction = style_profile["outline_style_instruction"][m
[36m@@ -202,13 +277,13 @@[m [mdef create_tasks([m
 [m
     # 1. 大纲动态优化师任务 (根据是否已有详细大纲决定是否跳过)[m
     outline_source = "大纲优化师生成的提纲"[m
[31m-    [m
[32m+[m
     if is_detailed_outline:[m
[31m-        # 已有详细大纲，跳过Agent 1[m
[32m+[m[32m        # 已有详细大纲，跳过 Agent 1[m
         outline_source = "上述【本章大纲指引】"[m
         # 不添加 task1[m
     else:[m
[31m-        # 没有详细大纲，需要AI生成[m
[32m+[m[32m        # 没有详细大纲，需要 AI 生成[m
         task1 = Task([m
             description=([m
                 f"请基于以下信息，为第{current_chapter}章生成详细提纲。\n"[m
[36m@@ -236,12 +311,12 @@[m [mdef create_tasks([m
         expected_output="本章相关的人物卡快照与世界观设定检查表（Markdown格式）"[m
     )[m
     tasks.append(task2)[m
[31m-    [m
[31m-    # 3. 章节主写手任务 (原Task 4)[m
[32m+[m
[32m+[m[32m    # 3. 章节主写手任务[m
     writer_instruction = ([m
         f"根据所有准备工作撰写{min_chars}-{max_chars}字正文。\n"[m
         "强制要求：\n"[m
[31m-        "0. 开头必须直接进入场景（动作/对白/冲突），禁止用“回顾/复盘/前情提要”式叙述；禁止出现‘上一章/上回/回想/不久前/转眼/与此同时’等总结承接句；禁止使用通用开场白（如天色/夜色/时间飞逝/几日后/一夜之间）。\n"[m
[32m+[m[32m        "0. 开头必须直接进入场景（动作/对白/冲突），禁止用"回顾/复盘/前情提要"式叙述；禁止出现'上一章/上回/回想/不久前/转眼/与此同时'等总结承接句；禁止使用通用开场白（如天色/夜色/时间飞逝/几日后/一夜之间）。\n"[m
         "1. 必须承接上一章末状态，并在本章前20%篇幅落地。\n"[m
         "2. 【核心约束】必须严格执行【本章大纲指引】中的剧情目标，不得随意偏离主线或无脑注水。\n"[m
         "3. 不得让角色状态回滚、瞬移、突然知道未出现信息。\n"[m
[36m@@ -261,6 +336,7 @@[m [mdef create_tasks([m
         f"{base_context}{prev_info}"[m
     )[m
     if compact_mode:[m
[32m+[m[32m        # 精简模式：失败降级时使用，减少约束以提高成功率[m
         writer_instruction = ([m
             "根据提纲与人物设定直接写出完整正文并保证节奏紧凑。\n"[m
             "开头必须直接进入场景（动作/对白/冲突），禁止复述前情与通用开场白。\n"[m
[36m@@ -285,8 +361,8 @@[m [mdef create_tasks([m
         expected_output=f"完整的章节正文（{min_chars}-{max_chars}字）"[m
     )[m
     tasks.append(task3)[m
[31m-    [m
[31m-    # 4. 终极审校专家任务 (原Task 5)[m
[32m+[m
[32m+[m[32m    # 4. 终极审校专家任务[m
     finalizer_instruction = ([m
         "作为主编，请先执行【一致性Gate】，再执行润色：\n"[m
         "1. 必须项：承接上一章末状态；【核心约束】必须检查正文是否覆盖了【本章大纲指引】的关键事件；人设/战力/世界规则不跳变；\n"[m
[36m@@ -309,6 +385,7 @@[m [mdef create_tasks([m
         agent=agents[3],[m
         expected_output="经审查与润色后的完整章节定稿，必须以 '# 第N章 章节名' 开头"[m
     )[m
[32m+[m
     tasks.append(task4)[m
[31m-    [m
[32m+[m
     return tasks[m
